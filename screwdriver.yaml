shared:
  image: node:20
jobs:
  main:
    requires: [~pr, ~commit]
    annotations:
      screwdriver.cd/dockerEntrypoint: ["/bin/sh", "-c"]
    steps:
      - export: export GREETING="Hello, world!"
      - hello: echo $GREETING
