shared:
  image: node:20-slim
jobs:
  main:
    requires: [~pr, ~commit]
    annotations:
      screwdriver.cd/dockerEntrypoint: ["/bin/sh", "-c"]
    steps:
      - export: export GREETING="Hello, world!"
      - hello: echo $GREETING
